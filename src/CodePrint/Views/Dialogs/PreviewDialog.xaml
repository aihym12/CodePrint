<Window x:Class="CodePrint.Views.Dialogs.PreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:CodePrint.Converters"
        Title="打印预览" Width="800" Height="600"
        WindowStartupLocation="CenterOwner" WindowState="Maximized"
        Background="#333">
    <Window.Resources>
        <converters:MmToPixelConverter x:Key="MmToPx"/>
        <converters:ColorStringToBrushConverter x:Key="ColorToBrush"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar -->
        <Border Grid.Row="0" Background="#444">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="打印预览" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,20,0"/>
                <TextBlock x:Name="SizeText" Foreground="#CCC" FontSize="13" VerticalAlignment="Center"/>
                <Button Content="关闭" Margin="20,0,0,0" Padding="16,6" Click="Close_Click"
                        Background="#E53935" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Preview area -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="40">
                <Border x:Name="PreviewBorder" Background="White" BorderBrush="#555" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="4" BlurRadius="12" Opacity="0.5"/>
                    </Border.Effect>
                    <Canvas x:Name="PreviewCanvas" ClipToBounds="True"/>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
